---
description: Produce a dated content calendar (ideas + keywords + intent) for Shopping Gems
argument-hint: [number of posts, default 8] [season or angle, optional]
---

Create a content plan for Shopping Gems.

1. `Glob` + skim `src/content/posts/*.md` (titles, categories, tags, dates) —
   list what we already cover so nothing duplicates.
2. Note the season from today's date; lean into upcoming sale events
   (Prime Day, 11.11, Black Friday, New Year, Anniversary, summer/winter sales).
3. Output a table: # · working title · primary keyword · search intent
   (informational/transactional) · category (existing preferred) · cluster it
   belongs to · 2 internal-link targets (real slugs).
4. Mix pillars (calendars, big guides) with quick wins (gift picks, deal explainers).
5. Keep it in chat — ask which items to write before creating anything.
